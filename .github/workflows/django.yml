name: Django CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_financetracker
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run migrations
      env:
        DATABASE_ENGINE: postgresql
        DATABASE_NAME: test_financetracker
        DATABASE_USER: postgres
        DATABASE_PASSWORD: postgres
        DATABASE_HOST: localhost
        DATABASE_PORT: 5432
        SECRET_KEY: test-secret-key-for-ci
      run: |
        python manage.py migrate
    
    - name: Run tests
      env:
        DATABASE_ENGINE: postgresql
        DATABASE_NAME: test_financetracker
        DATABASE_USER: postgres
        DATABASE_PASSWORD: postgres
        DATABASE_HOST: localhost
        DATABASE_PORT: 5432
        SECRET_KEY: test-secret-key-for-ci
      run: |
        python manage.py test

  build-docker:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Docker image
      run: |
        docker build -t financetracker:${{ github.sha }} .
    
    - name: Test Docker image
      run: |
        docker run --rm --entrypoint="" financetracker:${{ github.sha }} python manage.py check

  # Optional: Deploy stage (uncomment and configure for your deployment target)
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: [test, build-docker]
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - name: Deploy to production
  #     run: |
  #       echo "Add your deployment commands here"